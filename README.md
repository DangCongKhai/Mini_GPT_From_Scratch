# Mini GPT From Scratch
This repo is a complete tutorial that guides you to building a Large Language Model (LLM) from scratch starting from building a tokenizer using Byte Pair Encoding algorithm,
**self-attention** mechanism within Transformer, positional encoding to Decoder Architecture that used in GPT model.
